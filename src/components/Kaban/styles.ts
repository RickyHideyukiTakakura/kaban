import styled from 'styled-components';

export const Kaban = styled.div`
  display: flex;
  flex-direction: column;
  gap: 2.4rem;
  padding: 0 2.4rem 2.4rem 0;
`;

export const KabanHeader = styled.strong`
  font-weight: 700;
  font-size: 2rem;
  color: var(--brown-300);
`;
